<div class='col-md-6'>{{story.author}} -- {{story.human_time}}</div>
<div class='col-md-1'>{{story.score}}</div>
<div class='col-md-1'>
  <button class='fill-all' {{action 'toggleRead' story.id}}>
    {{#if story.read}}●{{else}}○{{/if}}
  </button>
</div>
<div class='col-md-1'>
  <!--TODO write a less brittle, state dependant function for this -->
  {{#if 'controllers.application.linkedToPocket'}}
    <button class='fill-all' {{action 'addCurrentToPocket'}}>P</button>
  {{/if}}
</div>
